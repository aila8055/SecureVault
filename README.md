# SecureVault
SecureVault is a smart password security assistant that generates strong, secure, and reproducible passwords from user input using SHA-256-based deterministic logic. It helps users create real-world strong passwords without needing to remember complex random passwords.
